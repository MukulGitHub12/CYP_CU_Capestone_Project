# Capestone - Crop Yield Prediction
CYP_APP.py is the GUI developed to deploy the model.

The cleaned Dataset used which inclused merged data from Public and IMD data reprocessed is attached as cleaned_Crop_data.csv.

The clean final Notebook is showcasing the final model used. 
